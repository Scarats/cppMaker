#ifndef CHECKSTRING_CLASS_HPP
#define CHECKSTRING_CLASS_HPP

#include <string>
#include <sstream>

bool checkExit(std::string &str);
bool checkIndex(int &size, std::string &str);
bool checkString(std::string const &str);
bool checkNumber(std::string const &str);

#endif
